# Azure DevOps standalone agent

Azure DevOps to run an standalone (self-hosted) pod in Kubernetes.

# Install

```sh
helm install azure-devops-agent --namespace azure-devops --create-namespace ./chart
```
